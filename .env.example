# Anthropic API key for the production transport. When absent, the dev
# fallback transport (Claude CLI headless) is used for model calls.
ANTHROPIC_API_KEY=

# Local embedded Postgres connection (set by scripts/db-fresh.ts defaults).
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5799/argo
